[Unit]
Description=Cloudflare Tunnel for Invitation App
After=network.target

[Service]
Type=simple
User=pi
ExecStart=/usr/local/bin/cloudflared tunnel run invitations
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
